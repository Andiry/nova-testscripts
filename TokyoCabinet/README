Tokyo Cabinet is a key-value database.

Install:
# apt-get install libtokyocabinet-dev
# make

Usage:
# cp tokyo_cabinet_set /mnt/ramdisk/
# cd /mnt/ramdisk
# ./tokyo_cabinet_set --iterations=1 --num-keys=100000 --value-size=1024

